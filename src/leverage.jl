# function StatsBase.leverage(model::LinearMixedModel) end

# function StatsBase.leverage(model::LinearMixedModel, group::AbstractString)
#     return leverage(model, Symbol(group))
# end

# function StatsBase.leverage(model::LinearMixedModel, group::Symbol) end